{
  "name": "n8n-nodes-easybill-api-root",
  "private": true,
  "type": "commonjs",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.15"
  },
  "scripts": {
    "build": "npx rimraf dist && tsc --incremental false && gulp build:icons && npm run clean:dist && npm run copy:dist",
    "clean:dist": "rm -f dist/tsconfig.tsbuildinfo",
    "copy:dist": "cp -r dist packages/unscoped/ && cp -r dist packages/scoped/ && cp README.md LICENSE.md packages/unscoped/ && cp README.md LICENSE.md packages/scoped/",
    "dev": "tsc --watch",
    "format": "prettier nodes credentials --write",
    "lint": "eslint nodes credentials --ext .ts",
    "lint:fix": "eslint nodes credentials --ext .ts --fix"
  },
  "devDependencies": {
    "@typescript-eslint/parser": "~8.32.0",
    "eslint": "^8.57.0",
    "eslint-plugin-n8n-nodes-base": "^1.16.3",
    "gulp": "^5.0.0",
    "prettier": "^3.7.4",
    "typescript": "^5.8.2",
    "rimraf": "^5.0.0"
  }
}
