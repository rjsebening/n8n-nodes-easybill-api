name: Publish

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  publish:
    name: Publish to npm
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 'lts/*'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Publish n8n-nodes-easybill-api
        run: npm publish --provenance --access public
        env:
          RELEASE_MODE: 'true'

      - name: Publish @rjsebening/n8n-nodes-easybill-api
        shell: bash
        run: |
          set -euo pipefail
          publish_dir="$(mktemp -d)"

          cp -R dist "${publish_dir}/dist"
          cp README.md LICENSE.md package.json "${publish_dir}/"

          npm pkg set --prefix "${publish_dir}" name="@rjsebening/n8n-nodes-easybill-api"
          npm publish "${publish_dir}" --provenance --access public --ignore-scripts
        env:
          RELEASE_MODE: 'true'
